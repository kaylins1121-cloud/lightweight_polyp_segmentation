model:
  name: unet

training:
  epochs: 100
  batch_size: 16
  lr: 0.0001
  weight_decay: 0.0001

data:
  root: ./dataset
  image_size: 352

validation:
  split: 0.2
  seed: 42

hardware:
  num_workers: 2

checkpoint:
  dir: ./checkpoints
